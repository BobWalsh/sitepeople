<p id="notice"><%= notice %></p>
<div class="min-h-screen bg-white transition duration-500 ease-in-out ">
  <div class="py-10">
    <header>
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <h1 class="text-3xl font-bold leading-tight text-gray-900">
          Site People
        </h1>
      </div>
    </header>
    <main>
      <div class="max-w-7xl mx-auto sm:px-6 lg:px-8">
        <!-- Replace with your content -->
        <div class="px-4 py-8 sm:px-0">
          <div class="border-4 border-dashed border-gray-200 rounded-lg h-96">
            <p class="text-xl mt-4">Import the Source.csv file (located in your code directory.):</p>
            <%= link_to "Import Source.csv", new_home_path, class:"mt-4 text-white inline-flex items-center px-2.5 py-1.5 border border-transparent text-xs font-medium rounded shadow-sm text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500" %>
            <% if @sites.count > 0 %>
              <div class="mt-4">
                <p> There are now <%= @sites.count %> Sites.</p>
                <p> There are now <%= @siteworkers.count %> People.</p>
              <% end %>
            </div>
          </div>
          <!-- /End replace -->
        </div>
      </main>
    </div>
  </div>
